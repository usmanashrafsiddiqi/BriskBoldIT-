
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: '"Montserrat","Comic Sans MS" sans-serif',  
    fontWeightBold: 900
  },
});

export default theme;
